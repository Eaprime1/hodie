interface:
  display_name: "Hodie"
  short_description: "Repo-specific patterns and workflows for hodie"
  default_prompt: "Use the hodie repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true