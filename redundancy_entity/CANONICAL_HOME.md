# Canonical home moved

This system's code and design docs (`redundancy_entity_analyzer.py`,
`prime_collapse.py`, `prime_search.py`, `redundancy_ranger_custody.py`,
`GRAVITY_CORE_MISSION.md`, and the rest) were copied to
**`eaprime1/gravitar`** on 2026-08-22 — the repo's own name matches this
concept exactly, and it was empty until now.

Nothing here was deleted or edited — this folder is untouched, per the
system's own "conservation bias — nothing deleted" principle. This file is
just a pointer, added additively.

`beasis_catalog.json` (the Dec 2025 run's 25MB scan-result data, tied to
the Pixel 8a's local `/storage/emulated/0/unexusi_beasis/` corpus) stayed
here — it's data, not code, and wasn't copied.

See `eaprime1/gravitar`'s `README.md` for the full picture, including open
questions about reconciling this system's "keep every duplicate" philosophy
with `eaprime1/duplicatus`'s opposite approach.

Carrier: `moav/hodie_moav_transfer_gravity_core.json`
