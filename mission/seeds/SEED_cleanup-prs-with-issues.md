# SEED: Cleanup PRs with issues

Status: SEED
Scope: internal
Origin: branch `copilot/cleanup-prs-with-issues` (found during `.eric/BRANCH_FINALIZATION_2026-07-19.md`)
Created: 20260719

## What

A branch named `copilot/cleanup-prs-with-issues` exists in `Eaprime1/hodie`, byte-identical to `main`, with no commits of its own and no PR ever opened from it. The name implies an intended sweep of open PRs that have lingering issues (failing checks, stale review threads, etc.) — never executed.

## Why

Unknown whether this is still relevant. At the time this seed was written, the repo has exactly one open PR with a known issue (#143 — Drive sync repair, open 2.5 months, doesn't fix the actual daily-failure root cause). If that's what this seed was originally aimed at, it may already be partially covered by the ongoing finalization/audit work; if it meant something broader, that scope was never written down anywhere findable.

## Target

internal — `Eaprime1/hodie`

## Acceptance

Unknown yet. First step is deciding whether this is still wanted at all, or whether it should be marked DROPPED as superseded by the finalization/audit passes already in motion.

---

∰ 20260719000000003
